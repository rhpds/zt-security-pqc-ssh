---
# SETUP — Module 1
# Standalone playbook mode (no main.yml required).
# Runs before the module starts to prepare the environment.
#
# Available extravars vary by infrastructure type.
# AgD v2 OCP: k8s_kubeconfig, student_ns, bastion_host, bastion_port,
#             bastion_user, bastion_password

- name: "Module 1 Setup"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "TODO: Implement setup logic"
      ansible.builtin.debug:
        msg: "Replace this task with your setup implementation"
